<!DOCTYPE html>
<html>
<head>
<title>Welcome to {{ ansible_hostname }}!</title>
<style>
    body { width: 80%; margin: 0 auto; font-family: sans-serif; text-align: center; }
    h1 { color: #333; }
    p { color: #555; }
    .info { background-color: #eee; border: 1px solid #ccc; padding: 10px; border-radius: 5px; }
</style>
</head>
<body>
    <h1>Hello from Ansible!</h1>
    <p>This page is served from the node:</p>
    <div class="info">
        <h2>Hostname: {{ ansible_hostname }}</h2>
        <p>IP Address: {{ ansible_default_ipv4.address }}</p>
    </div>

    <hr>
    <h3>Secret Information:</h3>
    <p>{{ secret_message }}</p>
    <p>Running app version: {{ app_version }}</p>
</body>
</html>
